# Instructions

    # install the virtualenv
    make bootstrap

    # download the data
    make data

    # save a newly intialized model
    make w

    # train the model
    make

    # visualize the model
    make openvis
